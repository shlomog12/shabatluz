# הנחיות לעבודה בפרויקט הזה

## Git commits

**אל תוסיף** לשום commit message (או תיאור PR) שום דבר מהבאים:
- שורת "Co-Authored-By: Claude..." או כל אזכור אחר שקלוד/AI כתב את הקוד.
- לינק לסשן, לשיחה, או לכלי (למשל claude.ai/code/session/...).
- כל הסבר/הערה על כך שקלוד היה שותף לכתיבת הקוד.

**כל commit message (כולל body) חייב להיות באנגלית**, לא בעברית — גם אם
השיחה איתי מתנהלת בעברית. תיאור *מה השתנה ולמה* בלבד, בלי אזכור של
הכלי שנעזרו בו לכתיבה.

## שפה בקוד ובתיעוד

הכלל: **תוכן פנים-קוד (comments, docstrings) ותיעוד למפתחים (README,
spec/) — באנגלית. תוכן שמוצג בפועל למשתמש הקצה (גבאי דובר עברית) —
בעברית.**

באנגלית תמיד:
- הערות JSDoc/inline בכל קובצי `src/*.js` ו-`tests/*.mjs`.
- `README.md` וכל מסמכי `spec/`.
- שמות משתנים/פונקציות/קבצים (כבר כך בקוד הקיים).
- commit messages (ראו למעלה).

בעברית תמיד (אל תתרגם!):
- טקסט גלוי ב-`index.html` (תוויות, כפתורים, כותרות).
- ערכי `CONFIG.DEFAULTS` ב-`src/config.js` (תוויות `lbl-*`) — אלה הן
  ברירות המחדל של טקסט ההודעה בפועל.
- מחרוזות התבנית של ההודעה עצמה ב-`src/domain/scheduleGenerator.js`
  (למשל `` `*לו"ז שבת ${parashaName}*` ``, `'*שבת שלום!*'`).
- טקסטים המוצגים בפועל למשתמש בזמן ריצה: `confirm()`, טקסט כפתור
  ההעתקה ומשוב ('✓ הועתק!' וכו').
- נתוני `shabbat_times.csv` (שמות פרשות).

לפני שינוי כל טקסט עברי בקוד — תבדוק קודם אם הוא בקטגוריה "מוצג
למשתמש" (השאר כמו שהוא) או "תיעוד למפתחים" (תרגם לאנגלית). אם לא
בטוחים, תשאלו.
