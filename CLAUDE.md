# הנחיות לעבודה בפרויקט הזה

## Git commits

**אל תוסיף** לשום commit message (או תיאור PR) שום דבר מהבאים:
- שורת "Co-Authored-By: Claude..." או כל אזכור אחר שקלוד/AI כתב את הקוד.
- לינק לסשן, לשיחה, או לכלי (למשל claude.ai/code/session/...).
- כל הסבר/הערה על כך שקלוד היה שותף לכתיבת הקוד.

**כל commit message (כולל body) חייב להיות באנגלית**, לא בעברית — גם אם
השיחה איתי מתנהלת בעברית. תיאור *מה השתנה ולמה* בלבד, בלי אזכור של
הכלי שנעזרו בו לכתיבה.

## שפה בקוד ובתיעוד

הכלל (זהה למוסכם בפרויקט האחות `calanderApp`), לפי **שלוש** קטגוריות
נפרדות — לא שתיים:

1. **קוד ומסמכי ארכיטקטורה/פונקציונליות עמוקים (`src/*.js`,
   `tests/*.mjs`, `spec/`) — תמיד באנגלית.** אלה מסמכים טכניים
   למפתחים בלבד.
2. **`README.md` — תמיד בעברית**, גוף כמו כותרת. זהו "דלת הכניסה"
   לפרויקט עבור הבעלים (דובר עברית), לא מסמך טכני עמוק — בדיוק כמו
   ב-`calanderApp/README.md`. **אל תתרגם אותו לאנגלית**, גם אם
   שאר התיעוד באנגלית. (טעות שכבר קרתה פעם: תרגום כל התוכן חוץ
   מהכותרת, מה שיצר קובץ מעורב מוזר.)
3. **תוכן שמוצג בפועל למשתמש הקצה (גבאי דובר עברית) — תמיד בעברית**:
   - טקסט גלוי ב-`index.html` (תוויות, כפתורים, כותרות).
   - ערכי `CONFIG.DEFAULTS` ב-`src/config.js` (תוויות `lbl-*`) — אלה
     הן ברירות המחדל של טקסט ההודעה בפועל.
   - מחרוזות התבנית של ההודעה עצמה ב-`src/domain/scheduleGenerator.js`
     (למשל `` `*לו"ז שבת ${parashaName}*` ``, `'*שבת שלום!*'`).
   - טקסטים המוצגים בפועל למשתמש בזמן ריצה: `confirm()`, טקסט כפתור
     ההעתקה ומשוב ('✓ הועתק!' וכו').
   - נתוני `shabbat_times.csv` (שמות פרשות).

commit messages — תמיד באנגלית (ראו למעלה), גם שהם לא באמת "מסמך
בפרויקט" — זה חריג נפרד, לא כלול בשלוש הקטגוריות.

לפני שינוי כל טקסט עברי — תבדוק תחילה איזו קטגוריה זו. `README.md`
הוא הטעות הכי קלה לחזור עליה: הוא *נראה* כמו תיעוד טכני אבל הוא
בקטגוריה של עצמו. אם לא בטוחים, תשאלו.
